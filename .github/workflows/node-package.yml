name: Node CI/CD

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]
  release:
    types: [created]

jobs:
  build:
    uses: actionsaustenstone/.github/.github/workflows/node-package.yml@main
    with:
      context: github
